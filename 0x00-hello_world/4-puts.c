#include <stdio.h>
#include <string.h>

int main(void)

{
        char string[100];
        strcpy(string, "Programming is like building a multilingual puzzle");
        puts(string);
        return 0;
}
