#include <stdio.h>
#include <string.h>
/**
 * main - main block
 * return: 0
 */

int main(void)
{
        char string[100];
        strcpy(string, "Programming is like building a multilingual puzzle\n");

        puts(string);
        return (0);
}
